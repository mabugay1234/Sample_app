<%= link_to gravatar_for(current_user, size: Settings.user_info) %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("user_info.view_profile"), current_user %></span>
<span><%= pluralize(current_user.microposts.count, "user_info.micropost") %></span>
